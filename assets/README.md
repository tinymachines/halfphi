# Assets

`halfphi.svg` is the mark used at the top of the README. Drop it here and it
appears there with no other change.

Prefer SVG: the README renders it at 180px wide on GitHub, and the same file
should survive being used larger elsewhere. If a raster fallback is wanted,
`halfphi.png` at 2x the intended display size beside it.
